# Brain teaser at the beginning of class

# Create a method that takes an array of intergers
# Separate each interger by even and odd numbers
# You don't need to worry about getting input form the user.


def odd_or_even(number)
  #   #Modulo % (returns the remainder)
  #   # == equality
  #   if number % 2 == 0
  #     'even' 
  #   else
  #      'odd'
  #   end
  # end

interger_arr = []

another_arr = [1, 2, 3, 4, 5]
# new_arr = another_arr. select { |num| num % 2 == 0 } 
# can change select for reject


def
  interger.select { |num| num % 2 == 0 }